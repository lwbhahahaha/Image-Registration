Registration program requires that NiftyReg is installed and an NVIDIA graphics card (CUDA).
To check if NiftyReg is installed, search for C:\Programs\NiftyReg\bin.  The following files 
must be present:

1) reg_aladin.exe
2) reg_f3d.exe

If either are missing, registration will not run properly.
